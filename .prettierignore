dist
dist-ssr
coverage
node_modules
package-lock.json

# Hand-maintained phase docs — Prettier's markdown reflow fights the deliberate
# wrapping and table alignment here.
docs

# Supabase SQL migrations + seed (Prettier has no SQL parser); Deno function is
# formatted to its own runtime's conventions.
supabase

