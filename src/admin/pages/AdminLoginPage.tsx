/**
 * Login placeholder. Real Supabase Auth (email + password, per ADR-0007) is Phase 3.
 */
export function AdminLoginPage() {
  return (
    <section>
      <h1 className="text-2xl font-bold tracking-tight">Admin sign in</h1>
      <p className="mt-3 text-sm text-platinum">
        Placeholder. Email + password sign-in (Supabase Auth) is built in Phase 3.
      </p>
    </section>
  );
}
