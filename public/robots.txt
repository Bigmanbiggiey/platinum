# Dev fallback only. The real robots.txt is generated per-environment at build time
# by scripts/finalize-seo.mjs (adds the Sitemap line, or Disallow: / for staging).
User-agent: *
Allow: /
Disallow: /admin
