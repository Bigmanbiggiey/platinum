# Copy to .env.local and fill in. .env.local is git-ignored — never commit real keys.
#
# Supabase project (owner's dev account for now). Both values are safe in the browser;
# Row Level Security is the security boundary (ADR-0004). No schema exists in Phase 1.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
